name: codestra-gateway-traditional
services:
  kong-proxy-1:
    read_only: true
    user: 1000:1000
    cap_drop: &id001
    - ALL
    security_opt: &id002
    - no-new-privileges:true
    pids_limit: 256
    mem_limit: 1g
    cpus: '1.0'
    restart: 'no'
    tmpfs: &id003
    - /tmp:rw,noexec,nosuid,size=64m
    - /usr/local/kong:rw,nosuid,size=256m,uid=1000,gid=1000
    image: ${KONG_PLATFORM_IMAGE_REPOSITORY:?reviewed gateway image repository required}@${KONG_PLATFORM_IMAGE_DIGEST:?immutable
      sha256 digest required}
    profiles:
    - traditional-source
    entrypoint:
    - /usr/local/bin/codestra-kong-entrypoint
    environment:
      KONG_PREFIX: /usr/local/kong
      KONG_ADMIN_LISTEN: 'off'
      KONG_ADMIN_GUI_LISTEN: 'off'
      KONG_STATUS_LISTEN: 0.0.0.0:8100
      KONG_PROXY_ACCESS_LOG: 'off'
      KONG_ADMIN_ACCESS_LOG: 'off'
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      KONG_LOG_LEVEL: warn
      KONG_ANONYMOUS_REPORTS: 'off'
      KONG_PLUGINS: bundled,codestra-authz,codestra-request-context,codestra-webhook-verifier
      KONG_LUA_SSL_TRUSTED_CERTIFICATE: /run/secrets/trust_ca
      KONG_DATABASE: postgres
      KONG_PG_HOST: kong-postgres.internal.codestra
      KONG_PG_PORT: '5432'
      KONG_PG_DATABASE: kong
      KONG_PG_USER: kong_runtime
      KONG_PG_PASSWORD_FILE: /run/secrets/kong_runtime_password
      KONG_PG_SSL: 'on'
      KONG_PG_SSL_VERIFY: 'on'
      KONG_PG_MAX_CONCURRENT_QUERIES: '20'
      KONG_ROLE: traditional
      KONG_PROXY_LISTEN: 0.0.0.0:8443 ssl
      KONG_SSL_CERT: /run/secrets/proxy_cert
      KONG_SSL_CERT_KEY: /run/secrets/proxy_key
      KONG_LICENSE_PATH: /run/secrets/kong_license
      KONG_STATUS_ACCESS_LOG: 'off'
      KONG_VAULTS: env
      KONG_TRUSTED_IPS: ${KONG_TRUSTED_IPS:?exact approved Caddy and upstream-gateway
        source CIDRs required}
      KONG_REAL_IP_HEADER: X-Forwarded-For
      KONG_REAL_IP_RECURSIVE: 'on'
    networks:
    - kong_proxy
    - kong_database
    - kong_rate_limit
    - observability
    - middleware_upstream
    secrets:
    - trust_ca
    - kong_runtime_password
    - proxy_cert
    - proxy_key
    - redis_password
    - kong_license
    - oidc_cache_tokens_salt
    command:
    - kong
    - docker-start
    volumes:
    - type: bind
      source: ${GATEWAY_WEBHOOK_SECRET_DIRECTORY:?approved per-integration webhook secret directory required}
      target: /run/secrets/webhooks
      read_only: true
  kong-proxy-2:
    read_only: true
    user: 1000:1000
    cap_drop: *id001
    security_opt: *id002
    pids_limit: 256
    mem_limit: 1g
    cpus: '1.0'
    restart: 'no'
    tmpfs: *id003
    image: ${KONG_PLATFORM_IMAGE_REPOSITORY:?reviewed gateway image repository required}@${KONG_PLATFORM_IMAGE_DIGEST:?immutable
      sha256 digest required}
    profiles:
    - traditional-source
    entrypoint:
    - /usr/local/bin/codestra-kong-entrypoint
    environment:
      KONG_PREFIX: /usr/local/kong
      KONG_ADMIN_LISTEN: 'off'
      KONG_ADMIN_GUI_LISTEN: 'off'
      KONG_STATUS_LISTEN: 0.0.0.0:8100
      KONG_PROXY_ACCESS_LOG: 'off'
      KONG_ADMIN_ACCESS_LOG: 'off'
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      KONG_LOG_LEVEL: warn
      KONG_ANONYMOUS_REPORTS: 'off'
      KONG_PLUGINS: bundled,codestra-authz,codestra-request-context,codestra-webhook-verifier
      KONG_LUA_SSL_TRUSTED_CERTIFICATE: /run/secrets/trust_ca
      KONG_DATABASE: postgres
      KONG_PG_HOST: kong-postgres.internal.codestra
      KONG_PG_PORT: '5432'
      KONG_PG_DATABASE: kong
      KONG_PG_USER: kong_runtime
      KONG_PG_PASSWORD_FILE: /run/secrets/kong_runtime_password
      KONG_PG_SSL: 'on'
      KONG_PG_SSL_VERIFY: 'on'
      KONG_PG_MAX_CONCURRENT_QUERIES: '20'
      KONG_ROLE: traditional
      KONG_PROXY_LISTEN: 0.0.0.0:8443 ssl
      KONG_SSL_CERT: /run/secrets/proxy_cert
      KONG_SSL_CERT_KEY: /run/secrets/proxy_key
      KONG_LICENSE_PATH: /run/secrets/kong_license
      KONG_STATUS_ACCESS_LOG: 'off'
      KONG_VAULTS: env
      KONG_TRUSTED_IPS: ${KONG_TRUSTED_IPS:?exact approved Caddy and upstream-gateway
        source CIDRs required}
      KONG_REAL_IP_HEADER: X-Forwarded-For
      KONG_REAL_IP_RECURSIVE: 'on'
    networks:
    - kong_proxy
    - kong_database
    - kong_rate_limit
    - observability
    - middleware_upstream
    secrets:
    - trust_ca
    - kong_runtime_password
    - proxy_cert
    - proxy_key
    - redis_password
    - kong_license
    - oidc_cache_tokens_salt
    command:
    - kong
    - docker-start
    volumes:
    - type: bind
      source: ${GATEWAY_WEBHOOK_SECRET_DIRECTORY:?approved per-integration webhook secret directory required}
      target: /run/secrets/webhooks
      read_only: true
  kong-management:
    read_only: true
    user: 1000:1000
    cap_drop: *id001
    security_opt: *id002
    pids_limit: 256
    mem_limit: 1g
    cpus: '1.0'
    restart: 'no'
    tmpfs: *id003
    image: ${KONG_PLATFORM_IMAGE_REPOSITORY:?reviewed gateway image repository required}@${KONG_PLATFORM_IMAGE_DIGEST:?immutable
      sha256 digest required}
    profiles:
    - traditional-source
    environment:
      KONG_PREFIX: /usr/local/kong
      KONG_ADMIN_LISTEN: 127.0.0.1:8001
      KONG_ADMIN_GUI_LISTEN: 'off'
      KONG_STATUS_LISTEN: 0.0.0.0:8100
      KONG_PROXY_ACCESS_LOG: 'off'
      KONG_ADMIN_ACCESS_LOG: 'off'
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      KONG_LOG_LEVEL: warn
      KONG_ANONYMOUS_REPORTS: 'off'
      KONG_PLUGINS: bundled,codestra-authz,codestra-request-context,codestra-webhook-verifier
      KONG_LUA_SSL_TRUSTED_CERTIFICATE: /run/secrets/trust_ca
      KONG_DATABASE: postgres
      KONG_PG_HOST: kong-postgres.internal.codestra
      KONG_PG_PORT: '5432'
      KONG_PG_DATABASE: kong
      KONG_PG_USER: kong_runtime
      KONG_PG_PASSWORD_FILE: /run/secrets/kong_runtime_password
      KONG_PG_SSL: 'on'
      KONG_PG_SSL_VERIFY: 'on'
      KONG_PG_MAX_CONCURRENT_QUERIES: '20'
      KONG_ROLE: traditional
      KONG_PROXY_LISTEN: 'off'
      KONG_LICENSE_PATH: /run/secrets/kong_license
      KONG_STATUS_ACCESS_LOG: 'off'
      KONG_VAULTS: env
    networks:
    - kong_admin
    - kong_database
    - observability
    secrets:
    - trust_ca
    - kong_runtime_password
    - redis_password
    - kong_license
    - oidc_cache_tokens_salt
    entrypoint:
    - /usr/local/bin/codestra-kong-entrypoint
    command:
    - kong
    - docker-start
    volumes:
    - type: bind
      source: ${GATEWAY_WEBHOOK_SECRET_DIRECTORY:?approved per-integration webhook secret directory required}
      target: /run/secrets/webhooks
      read_only: true
networks:
  edge_internal:
    external: true
    name: codestra_edge_internal
  kong_proxy:
    external: true
    name: codestra_kong_proxy
  kong_cluster:
    external: true
    name: codestra_kong_cluster
  kong_admin:
    external: true
    name: codestra_kong_admin
  kong_database:
    external: true
    name: codestra_kong_database
  kong_rate_limit:
    external: true
    name: codestra_kong_rate_limit
  observability:
    external: true
    name: codestra_observability
  middleware_upstream:
    external: true
    name: codestra_middleware_upstream
  backup_authority:
    external: true
    name: codestra_backup_authority
secrets:
  trust_ca:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/trust_ca
  kong_runtime_password:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/kong_runtime_password
  cluster_ca:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/cluster_ca
  cp_cert:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/cp_cert
  cp_key:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/cp_key
  dp1_cert:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/dp1_cert
  dp1_key:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/dp1_key
  dp2_cert:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/dp2_cert
  dp2_key:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/dp2_key
  proxy_cert:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/proxy_cert
  proxy_key:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/proxy_key
  redis_password:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/redis_password
  kong_license:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/kong_license
  oidc_cache_tokens_salt:
    file: ${GATEWAY_SECRET_DIRECTORY:?approved local secret directory required}/oidc_cache_tokens_salt
