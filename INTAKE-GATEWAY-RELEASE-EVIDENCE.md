# Unified Intake Gateway Release Evidence

This root-level evidence marker intentionally sits outside the migration authority inventory. It exists only to trigger an exact-head pull-request validation after GitHub Actions regenerated the deterministic migration manifests.

Canonical path: `Caddy -> Kong -> Middleware -> Odoo`.

No runtime Kong apply, deployment, credential mutation, DNS change, or production traffic change is authorized by this file.
