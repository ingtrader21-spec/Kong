# Order is security-sensitive. No trust authentication is permitted.
local   all          all                                      scram-sha-256
host    kong         kong_runtime          10.254.42.0/28      scram-sha-256
host    kong         kong_migration_admin  10.254.42.0/28      scram-sha-256
host    kong         kong_monitor          10.254.42.0/28      scram-sha-256
host    replication  kong_backup           10.254.42.0/28      scram-sha-256
# Replication CIDR is rendered by the approved deployment from its private
# infrastructure inventory; fail closed if it is absent.
hostssl replication  kong_replication      ${REPLICA_CIDR}     scram-sha-256
host    all          all                   0.0.0.0/0            reject
host    all          all                   ::/0                 reject
